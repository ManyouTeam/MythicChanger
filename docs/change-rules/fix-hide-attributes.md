# Fix Hide Attributes

## Fix Hide Attributes

Hide Attribute item flag has bug in 1.20.6 higher version, this rule can try fix the items that the hide attribute flag does not work in some items.

```yaml
fake-changes:
  fix-hide-attributes: true
```

